// import { observable } from "mobx";

// class store {
//   @observable modalShow = false;
//   async getList() {
//     await http(`/api/xxx/`).then(res => {
//       const list = res.data || []
//       runInAction(() => {
//         this.modalShow = list.length > 0 ? true : false
//       })
//     })
//   }
// }
